<Window x:Class="Dateien_Sortierprogramm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dateien_Sortierprogramm"
        xmlns:vm="clr-namespace:Dateien_Sortierprogramm.ViewModels"
        xmlns:converters="clr-namespace:Dateien_Sortierprogramm.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
        </Style>
    </Window.Resources>
    <Grid>
        <!--GridDefinition-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <!--Labels-->
        <Label x:Name="lbl_TargetFolder" Grid.Row="1" Grid.Column ="1"  VerticalAlignment="Top" HorizontalAlignment="Left" Height="30" Width="100" Content="Zielordner" Margin="10,35,0,0"/>
        <Label x:Name="lbl_Searchterm" Grid.Row="1" Grid.Column="0"  VerticalAlignment="Top" HorizontalAlignment="Left" Height="30" Width="100" Content="Suchwort" Margin="10,35,0,0"/>

        <!--TextBoxes-->
        <TextBox x:Name="txt_Keyword"  Margin="10,70,10,10" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" />
        <TextBox x:Name="txt_TargetFolder" Width="200" Margin="10,70,50,10" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Top"        />

        <!--Buttons-->

        <Button x:Name="btn_AddTargetFolder" VerticalAlignment ="Top" Margin="214,72,2,0" Height="20" Width="30" Grid.Row="1" Grid.Column="1" />
        <Button x:Name="btn_Add" Command="{Binding TestHelloCommand}" VerticalAlignment="Top" Margin="10,70,10,10" Height="30" Grid.Row="1" Grid.Column="2"  Content="Hinzufügen"     />

        <Button x:Name ="btn_Load" Margin="10"  Height="30" Grid.Row="2" Grid.Column="0"  Content="Laden"          />
        <Button x:Name="btn_Save" Margin="10"  Height="30" Grid.Row="2" Grid.Column="1"    Content="Speichern"      />
        <Button x:Name ="btn_Start" Margin="10"  Height="30" Grid.Row="2" Grid.Column="2"  Content="Sortiervorgang starten"       />

        <!--DataGrid oben: Ordner in denen gesucht werden soll-->
        <DataGrid Margin="5" Grid.ColumnSpan="2" x:Name="dgSourceFolders" CanUserAddRows="True" ItemsSource="{Binding SourceFolders}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ordnerpfad" Width="*">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button x:Name="btn_AddNewSource"
                                            DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                            Command="{Binding TestHelloCommand}"
                                            Content="+ Ordnerpfad hinzufügen" />
                                            <TextBlock Text="{Binding FolderPath}" Margin="5,0,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>


        <!--DataGrid Mitte: Suchdefinition-->
        <DataGrid x:Name="dg_Sorting"  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="10,149,10,0" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="Suchwort" Width="*" />
                <DataGridTextColumn Header="Zielordner" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
